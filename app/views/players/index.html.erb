<!-- Example row of columns -->
<body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Project name</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right">
            <div class="form-group">
              <input type="text" placeholder="Email" class="form-control">
            </div>
            <div class="form-group">
              <input type="password" placeholder="Password" class="form-control">
            </div>
            <button type="submit" class="btn btn-success">Sign in</button>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <!-- Main jumbotron for a primary marketing message or call to action -->

    </div>
      <div class="row">
        <div class="col-md-4">
          <h2>View All Players</h2>
          <p>

          </p>
          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
        </div>
        <div class="col-md-4">
          <h2>Find Players by League</h2>
            <img src="/images/leagues_map.jpg" alt="All Positions" height="300" width="300" />

          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
       </div>

        <div class="col-md-4">
          <h2>Find Players by Position</h2>
          <img src="/images/positions.png" alt="All Positions" height="300" width="300" />
          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
        </div>
      </div>





<br />
<br />
<table>
  <tr>
    <th></th>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Position</th>
    <th>Team</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  <tbody>
    <% @players.each do |player| %>
      <tr>
        
        <td><%= image_tag player.photo.url(:small) %></td>
        <td><%= link_to(player.first_name, player_path(player)) %></td>
        <td><%= player.last_name %></td>
        <td><%= player.position.name %></td>

        <td><%= player.team.name %></td>
        <td><%= link_to "edit", edit_player_path(player) %></td>
        <td><%= link_to "delete", player_path(player), method: :delete, data: {confirm: "Really want to delete this player?"}%></td>
      </tr>
    <% end %>
  </tbody>

<%= link_to "Create a New Player", new_player_path %>
<%= link_to "Create a New Team", new_team_path %>




