<%= link_to "Back to All Positions", positions_path %>

<p>
  <strong>Position:</strong> <%= @position.name %>
</p>

<h3>Players</h3>

<% @players.each do |player| %>
      <tr>
        
        <td><%= image_tag player.photo.url(:small) %></td>
        <strong><td><%= link_to(player.first_name, player_path(player)) %></td>
        <td><%= link_to(player.last_name, player_path(player)) %></td>
        <td>&nbsp 
        </td>
        <td>&nbsp 
        </td>
        <td>&nbsp 
        </td>
        <td><%= link_to(player.team.name, team_path(player.team)) %></td>
        </strong><br><br>
<% end %>

<td><%= link_to "edit", edit_position_path(@position) %></td>
